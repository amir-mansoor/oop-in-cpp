#include <iostream>
using namespace std;

float sqrt(float n); // function declaration only

int main() {
    float s;
    s = sqrt(36.0);
    cout << s << endl;
    return 0;
}

float sqrt(float n) {
    return 6.0;
}